<%= form_for(@dynamic_project_page) do |f| %>
  <% if @dynamic_project_page.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dynamic_project_page.errors.count, "error") %> prohibited this dynamic_project_page from being saved:</h2>

      <ul>
      <% @dynamic_project_page.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
